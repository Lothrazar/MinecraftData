{
    "block states": {
		"hardness": {
			"minecraft:default": "FLOAT: the time it takes to break this block with the fist or any tool not specified",
			"item_name": "FLOAT: the time it takes to break this block with the item specified"
		},
		"gravity": "BOOLEAN: if this block is affected by gravity",
		"burnability": "INTEGER: if it"s capable of burning it'll also burn up, unless the value is -1, 0 means cannot burn, 1 means can burn",
		"light": "INTEGER: the amount of light this block produces, if negative reduces light coming through the block by that amount",
		"light_needed": {
			"min": "INTEGER: minimum light",
			"max": "INTEGER: maximum light"
		},
		"tool_damage": {
			"item_name": "INTEGER: the amount of damage this item will get, when used to break this block"
		},
		"solid_faces": {
			"down": "BOOLEAN: if this face can be used as support",
			"up": "BOOLEAN: if this face can be used as support",
			"north": "BOOLEAN: if this face can be used as support",
			"south": "BOOLEAN: if this face can be used as support",
			"east": "BOOLEAN: if this face can be used as support",
			"west": "BOOLEAN: if this face can be used as support"
		},
		"need_support": {
			"down": "STRING: if this block needs support of a block here, empty if any solid faced block will suffice as support, negated if not needed (supports block states), can also be a list of strings",
			"up": "STRING: if this block needs support of a block here, empty if any solid faced block will suffice as support, negated if not needed (supports block states), can also be a list of strings",
			"north": "STRING: if this block needs support of a block here, empty if any solid faced block will suffice as support, negated if not needed (supports block states), can also be a list of strings",
			"south": "STRING: if this block needs support of a block here, empty if any solid faced block will suffice as support, negated if not needed (supports block states), can also be a list of strings",
			"east": "STRING: if this block needs support of a block here, empty if any solid faced block will suffice as support, negated if not needed (supports block states), can also be a list of strings",
			"west": "STRING: if this block needs support of a block here, empty if any solid faced block will suffice as support, negated if not needed (supports block states), can also be a list of strings"
		},
		"resistence": "FLOAT: how much explosion power it needs before getting destroyed",
		"transparent": "BOOLEAN: if this block is transparent (doesn't block redstone wire/doesn't provide power through the block/can have transparent textures)",
		"solid": "BOOLEAN: if this block is solid (taking damage when in your head when in the bounding box)",
		"hurt_on_touch": "STRING: a damage source + selector (+nbt data) to hurt (minecraft:fire @e[r=1] {Inventory:[{id:"minecraft:totem_of_undying"}]})",
        "piston_push": "INTEGER: wheather a piston can push this block; 0 = not pushable, 1 = pushable, -1 = breaks on push, 2 = pulls other blocks with it too",
        "sticky_push": "INTEGER: wheather another block that has piston_push set to 2 can push this block; 0 = not pushable, 1 = pushable, -1 = breaks on push, 2 = pulls other blocks with it too",
		"fall_damage": "FLOAT: 1 = 100%, 0.2 = 20%, -1 = bounce off",
           "hitbox": [
			"COMPOUND: basically a server-side model to use to calculate the hitbox"
		],
		"boundingbox": [
			"COMPOUND: basically a server-side model to use to calculate the collision"
		]
	}
}